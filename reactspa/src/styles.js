import { styled } from "baseui";

const Centered = styled('div', {
    display: 'flex',
    justifyContent: 'center',
    width: '100%',
    alignItems: 'center',
    height: '100%',
});

export { Centered }